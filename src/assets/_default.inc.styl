html
  font-size 62.5% // 10px

body
  font-family mainFont
  font-size 1.6rem
  min-width 32rem
  line-height 1.2
  overflow-x hidden

a
  text-decoration none
  color inherit

img
  max-width 100%
  height auto

textarea
  resize none

[hidden]
  display none

::selection
  background mainColor
  text-shadow none

// normalize-css()
reset()

.wrapper
  wrapper()
